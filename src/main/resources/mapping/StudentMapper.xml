<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.libra.spring.demo.dao.StudentMapper">

    <resultMap id="BaseResultMap" type="com.libra.spring.demo.model.entity.Student">
        <result column="id" jdbcType="BIGINT" property="id" />
        <result column="userName" jdbcType="VARCHAR" property="name" />
    </resultMap>

    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        id, `name`
    </sql>

    <select id="sel" resultType="com.libra.spring.demo.model.entity.Student">
        select * from student where id = #{id}
    </select>

    <select id="sql" resultType="com.libra.spring.demo.model.entity.Student">
        select * from student where id = #{id}
    </select>

    <!--auto generated by MybatisCodeHelper on 2021-05-24-->
    <select id="selectByNameAndId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from student
        <where>
            <if test="name != null">
                and `name`=#{name,jdbcType=VARCHAR}
            </if>
            <if test="id != null">
                and id=#{id,jdbcType=BIGINT}
            </if>
        </where>
    </select>

</mapper>
